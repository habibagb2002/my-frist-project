on: 
  push:
    branches:
       - master



jobs:
  build: 
     runs-on: ubuntu-t-latest 


     steps:
       - name: 'before'
         run: ls

       - uses: actions/checkout@v3
 
       - name: 'after'
         run: ls
