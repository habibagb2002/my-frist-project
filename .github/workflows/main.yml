on: 
  push:
    branches:
       - master



jobs:
  build: 
     runs-on: ubuntu-latest 


     steps:
      - name: 'after'
        run: ls
      
      -uses: actions/checkout@v3

      - name: 'before'
        run: ls

     
 
 
        



